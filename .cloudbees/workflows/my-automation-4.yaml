apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My Automation
on:
  push:
    branches:
      - ashok

jobs:
  Checkout:
    steps:
      - name: Checkout Source Code
        uses: cloudbees-io/checkout@v1

  Build:
    needs: Checkout
    steps:
      - name: Configure Git
        uses: docker://alpine/git:latest
        run: |
          git config --global --add safe.directory /cloudbees/workspace

      - name: Ensure Commit Contains JIRA Issue Key
        uses: docker://alpine/git:latest
        run: |
          if ! git log -1 --pretty=%B | grep -qE 'PROJECT-[0-9]+'; then
            echo "Commit does not contain a valid JIRA issue key CBDEMO-128."
            exit 1
          fi
        # This checks the latest commit message for a valid JIRA issue key format.

      - name: Build
        uses: docker://maven:3-eclipse-temurin-17
        run: |
          mvn compile

  
