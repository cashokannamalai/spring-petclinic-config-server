apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My automation
on:
  push:
    branches:
      - main
jobs:
  ValidateCommits:
    steps:
      - name: Configure git
        id: info
        uses: docker://alpine/git:latest
        run: |
          git config --global --add safe.directory /cloudbees/workspace

      - name: Fetch Commit Messages
        id: fetch-commits
        uses: docker://alpine/git:latest
        run: |
          cd /cloudbees/workspace  # Ensure we are in the repository directory
          git fetch --depth=2
          git log --pretty=format:"%s" HEAD..HEAD~1 > commit_messages.txt

      - name: Validate Jira Issue Keys
        id: validate-jira
        uses: docker://alpine/git:latest
        run: |
          cd /cloudbees/workspace  # Ensure we are in the repository directory
          grep -Eo 'CBDEMO-135' commit_messages.txt || (echo "Jira issue key missing in commit message" && exit 1)

  Build:
    needs: ValidateCommits
    steps:
      - name: Checkout
        id: Checkout
        uses: cloudbees-io/checkout@v1

      - name: Build
        id: Buildinfo
        uses: docker://maven:3-eclipse-temurin-17
        run: |
          cd /cloudbees/workspace  # Ensure we are in the repository directory
          mvn clean install
